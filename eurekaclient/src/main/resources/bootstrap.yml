spring:
  cloud:
    config:
      name: eurekaclient
      label: master
      discovery:
        enabled: true
        serviceId: springconfig
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
name: liqingjun
age: 18
management:
  endpoints:
    web:
      exposure:
        include: bus-refresh
